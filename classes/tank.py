import math, pygame

class tank():
    def __init__(typeofplayer, x, terain, screen, sw, sh):
        tempx, tempy = terain.find(x)
        self.typeofplayer, self.x, self.y, self.screen, self.sw, self.sh = typeofplayer, tempx, tempy, screen, sw, sh

    def gravity():
        
    
    def process():
